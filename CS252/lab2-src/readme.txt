name: Joseph Martella

login: jmartel

Features that work: 
	- backup with formatted timestamp
	- can detect changes in the directory (and create new backup)
	- limits backups to the number specified by user (deleting oldest)
	- emails user each time a backup is created


Features that don't work:
	- NONE

Extra features:
	- (may not be extra) checks amount of backups on startup and deletes 
	  as many as required to get amount down to max backups specified by user
